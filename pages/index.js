import React from 'react';
import './index.scss';

const Home = () => {
  return (
    <div class="container">
      <button type="button" class="btn btn-primary">
        Primary
      </button>
    </div>
  );
};

export default Home;
